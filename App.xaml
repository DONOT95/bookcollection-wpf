<Application xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BookCollection"
             xmlns:vm="clr-namespace:BookCollection.ViewModel"
             StartupUri="View/MainWindow.xaml">
    <Application.Resources>
        <!-- ================= COLORS ================= -->
        <Color x:Key="C_Background">#FFF7F8FA</Color>
        <Color x:Key="C_Surface">#FFFFFFFF</Color>
        <Color x:Key="C_Surface2">#FFF2F4F7</Color>
        <Color x:Key="C_Border">#FFE2E8F0</Color>
        <Color x:Key="C_Text">#FF0F172A</Color>
        <Color x:Key="C_TextMuted">#FF64748B</Color>

        <Color x:Key="C_Primary">#60A5FA</Color>
        <Color x:Key="C_PrimaryHover">#3B82F6</Color>

        <Color x:Key="C_Danger">#FDBA74</Color>
        <Color x:Key="C_DangerHover">#FB923C</Color>

        <SolidColorBrush x:Key="BgBrush" Color="{StaticResource C_Background}" />
        <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource C_Surface}" />
        <SolidColorBrush x:Key="Surface2Brush" Color="{StaticResource C_Surface2}" />
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource C_Border}" />
        <SolidColorBrush x:Key="TextBrush" Color="{StaticResource C_Text}" />
        <SolidColorBrush x:Key="MutedBrush" Color="{StaticResource C_TextMuted}" />
        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource C_Primary}" />
        <SolidColorBrush x:Key="PrimaryHoverBrush" Color="{StaticResource C_PrimaryHover}" />
        <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource C_Danger}" />
        <SolidColorBrush x:Key="DangerHoverBrush" Color="{StaticResource C_DangerHover}" />

        <!-- ================= WINDOW ================= -->
        <Style TargetType="Window">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="{StaticResource BgBrush}"/>
            <Setter Property="TextElement.Foreground" Value="{StaticResource TextBrush}"/>
        </Style>

        <!-- ================= DATAGRID LOOK ================= -->

        <!-- Text in cells (links, ellipsis) -->
        <Style x:Key="DG_CellText" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="None"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Padding" Value="2 0"/>

        </Style>

        <!-- Numbers in cells (zentriert oder rechts) -->
        <Style x:Key="DG_CellNumber" TargetType="TextBlock" BasedOn="{StaticResource DG_CellText}">
            <Setter Property="TextAlignment" Value="Left"/>
        </Style>

        <!-- Column header -->
        <Style x:Key="DG_Header" TargetType="DataGridColumnHeader">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        </Style>

        <!-- Cell container -->
        <Style x:Key="DG_Cell" TargetType="DataGridCell">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <!-- Normal Textfarbe -->
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}"/>

            <Style.Triggers>
                 <!--Hover--> 
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource BgBrush}"/>
                </Trigger>

                 <!--Selected: Hintergrund + Textfarbe-->
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Row -->
        <Style x:Key="DG_Row" TargetType="DataGridRow">
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="Background" Value="Transparent"/>
            <!-- Bottom border for rows -->
            <!--<Setter Property="BorderThickness" Value="0 0 0 1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>-->
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource BgBrush}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource BgBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ================= GROUPBOX CARD ================= -->
        <Style x:Key="CardGroupBox" TargetType="GroupBox">
            <Setter Property="Margin" Value="0,0,0,14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Border Background="{StaticResource SurfaceBrush}"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="12">

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="{TemplateBinding Header}"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,10"/>

                                <ContentPresenter Grid.Row="1"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Input Counter style for normal, warning and critical state (UI counter color cange) -->
        <Style x:Key="CounterTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}"/>

            <Style.Triggers>
                <!-- Binding Tag for reusable style (tag bind the property from vm)-->
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}"
                     Value="{x:Static vm:InputState.Warning}">
                    <Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
                </DataTrigger>

                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}"
                     Value="{x:Static vm:InputState.Critical}">
                    <Setter Property="Foreground" Value="{StaticResource DangerHoverBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- ================= TEXTBOX ================= -->
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="Background" Value="{StaticResource Surface2Brush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bd"
                                        Property="BorderBrush"
                                        Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ================= BASE BUTTON ================= -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Margin" Value="2 0"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="Background" Value="{StaticResource Surface2Brush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">

                            <ContentPresenter
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                           
                            <!-- Pressed -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd"
                                Property="Opacity"
                                Value="0.8"/>
                            </Trigger>

                            <!-- Disabled -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.8"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ================= PrimaryBtn ================= -->
        <Style x:Key="PrimaryButton"
               TargetType="Button"
               BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryHoverBrush}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ================= Hover ADDON ================= -->
        <Style x:Key="ModernButton_Hover" TargetType="Button"
                BasedOn="{StaticResource ModernButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ================= DeleteBtn ================= -->
        <Style x:Key="DangerButton"
               TargetType="Button"
               BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DangerBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource DangerHoverBrush}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource DangerHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ================= GATED BUTTONS ================= -->
        <!-- Enable,disable btn with the help of boolean IsSelected value (and relayCommands) -->
        <Style x:Key="ModifyButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource ModernButton}">
            <Setter Property="IsEnabled" Value="False"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                </Trigger>
                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter Property="IsEnabled" Value="True"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DeleteButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource DangerButton}">
            <Setter Property="IsEnabled" Value="False"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter Property="IsEnabled" Value="True"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Application.Resources>
</Application>
